def something(N, A):
    count = 0
    while True:
        for i in range(len(A)):
            if A[i] % 2 == 0:
                A[i] //= 2
            else:
                return count
        count += 1

N = input()
A = list(map(int, input().split()))
print(something(N, A))
